/**
 * src/app/composer/page.tsx
 * MOCK / VISUAL PROTOTYPE ONLY
 * Composer page
 */

import ComposerMock from "@/components/composer/ComposerMock";

export const metadata = {
  title: "The Composer — Duchess & Butler",
  description: "Compose your table interactively",
};

export default function ComposerPage() {
  return <ComposerMock />;
}
